version: '3.8'

services:
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./med-back
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./med-back/.env
